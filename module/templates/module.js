angular.module('<%= _.camelize(name) %>', ['ui.router','ngAnimate']);

angular.module('<%= _.camelize(name) %>').config(['$stateProvider',function($stateProvider) {

    /* Add New States Above */
}]);
